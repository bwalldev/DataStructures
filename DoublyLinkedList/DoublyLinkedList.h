// Doubly Linked List implementation

#include "Node.h"

template <typename T>
class DoublyLinkedList {
    private:
        int length;
    public:
        //DoublyLinkedList();
        //~DoublyLinkedList();
};